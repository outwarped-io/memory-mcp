# Migrations

Alembic-managed schema migrations.

Phase 0: empty (no revisions yet).
Phase 1 will land the v1 schema as a single revision (`v1_initial`) covering
everything in `design.md` → "Schema (Postgres) — Final v1".
